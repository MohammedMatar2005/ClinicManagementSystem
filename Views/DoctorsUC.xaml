<UserControl x:Class="ClinicManagementApplication.Views.DoctorsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ClinicManagementApplication.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000" FlowDirection="RightToLeft">

    <UserControl.DataContext>
        <vm:DoctorsViewModel />
    </UserControl.DataContext>

    <Grid Background="#F0F3F8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Height="4" Background="#3498DB" CornerRadius="2" Margin="20,5">
            <Border.Effect>
                <DropShadowEffect Color="#3498DB" BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
            </Border.Effect>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="15" Margin="20,10" Padding="25">
            <StackPanel>
                <Grid Margin="0,0,0,20">
                    <TextBlock Text="إعداد ملف الطبيب الجديد" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding CurrentTabIndex, StringFormat='المرحلة {0} من 2'}" Foreground="#3498DB" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                </Grid>

                <TabControl SelectedIndex="{Binding CurrentTabIndex}" BorderThickness="0" Background="Transparent">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </Style>
                        <Style TargetType="TextBlock" x:Key="FieldLabel">
                            <Setter Property="Foreground" Value="#5D7A95"/>
                            <Setter Property="Margin" Value="0,0,0,5"/>
                            <Setter Property="FontWeight" Value="Medium"/>
                        </Style>
                    </TabControl.Resources>

                    <TabItem>
                        <StackPanel>
                            <UniformGrid Columns="4">
                                <StackPanel Margin="5">
                                    <TextBlock Text="الاسم الأول" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.UserInfo.PersonInfo.FirstName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="اسم الأب" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.UserInfo.PersonInfo.SecondName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="اسم الجد" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.UserInfo.PersonInfo.ThirdName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="الكنية" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.UserInfo.PersonInfo.LastName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                            </UniformGrid>
                            <UniformGrid Columns="3" Margin="0,10,0,0">
                                <StackPanel Margin="5">
                                    <TextBlock Text="تاريخ الميلاد" Style="{StaticResource FieldLabel}"/>
                                    <DatePicker SelectedDate="{Binding SelectedDoctor.UserInfo.PersonInfo.DateOfBirth, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="الجنس" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox SelectedIndex="{Binding SelectedDoctor.UserInfo.PersonInfo.Gender, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center">
                                        <ComboBoxItem Content="ذكر"/>
                                        <ComboBoxItem Content="أنثى"/>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="رقم الهاتف" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.UserInfo.PersonInfo.Phone, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </TabItem>

                    <TabItem>
                        <StackPanel>
                            <UniformGrid Columns="3">
                                <StackPanel Margin="5">
                                    <TextBlock Text="اسم المستخدم" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.UserInfo.Username, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="كلمة المرور" Style="{StaticResource FieldLabel}"/>
                                    <PasswordBox Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="الدور" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox SelectedValue="{Binding SelectedDoctor.UserInfo.RoleId, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center">
                                        <ComboBoxItem Content="طبيب"/>
                                        <ComboBoxItem Content="مدير نظام"/>
                                    </ComboBox>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </TabItem>

                    <TabItem>
                        <StackPanel>
                            <UniformGrid Columns="3">
                                <StackPanel Margin="5">
                                    <TextBlock Text="رقم الرخصة" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.LicenseNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="التخصص" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.Specialization, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                                <StackPanel Margin="5">
                                    <TextBlock Text="الراتب" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding SelectedDoctor.Salary, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center" Padding="8"/>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </TabItem>
                </TabControl>

                <Grid Margin="10,25,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Command="{Binding BackCommand}" Content="السابق" Width="100" Height="42" Background="#BDC3C7" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>
                        <Button Command="{Binding NextCommand}" Content="التالي" Width="100" Height="42" Background="#3498DB" Foreground="White" FontWeight="Bold"/>
                        <Button Command="{Binding SaveCommand}" Content="حفظ نهائي" Width="140" Height="42" Background="#2ECC71" Foreground="White" FontWeight="Bold" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="15" Margin="20,10,20,20" Padding="15">
            <DataGrid ItemsSource="{Binding Doctors}" SelectedItem="{Binding SelectedDoctor}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="كود" Binding="{Binding DoctorId}" Width="60"/>
                    <DataGridTextColumn Header="اسم الطبيب" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="التخصص" Binding="{Binding Specialization}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>