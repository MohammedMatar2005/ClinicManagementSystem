<UserControl x:Class="ClinicManagementApplication.PatientsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ClinicManagementApplication.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1000" FlowDirection="RightToLeft">

    <UserControl.DataContext>
        <vm:PatientsViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F0F3F8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Height="4" Background="#2ECC71" CornerRadius="2" Margin="20,0">
            <Border.Effect>
                <DropShadowEffect Color="#2ECC71" BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
            </Border.Effect>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="15" Margin="20,10" Padding="25">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="إدارة بيانات المرضى" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                <UniformGrid Columns="4">
                    <StackPanel Margin="10,5">
                        <TextBlock Text="اسم المريض بالكامل" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SelectedPatient.FullName, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center" Padding="8" BorderBrush="#DCDFE6"/>
                    </StackPanel>

                    <StackPanel Margin="10,5">
                        <TextBlock Text="البريد الإلكتروني" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SelectedPatient.Email, UpdateSourceTrigger=PropertyChanged}" Height="38" VerticalContentAlignment="Center" Padding="8" BorderBrush="#DCDFE6"/>
                    </StackPanel>

                    <StackPanel Margin="10,5">
                        <TextBlock Text="فصيلة الدم" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <ComboBox SelectedValue="{Binding SelectedPatient.BloodType}" SelectedValuePath="Content" Height="38" VerticalContentAlignment="Center" BorderBrush="#DCDFE6">
                            <ComboBoxItem Content="A+"/>
                            <ComboBoxItem Content="A-"/>
                            <ComboBoxItem Content="B+"/>
                            <ComboBoxItem Content="B-"/>
                            <ComboBoxItem Content="O+"/>
                            <ComboBoxItem Content="O-"/>
                            <ComboBoxItem Content="AB+"/>
                            <ComboBoxItem Content="AB-"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="10,5">
                        <TextBlock Text="الحالة" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <CheckBox IsChecked="{Binding SelectedPatient.IsActive}" VerticalAlignment="Center" Content="نشط" Margin="0,10,0,0"/>
                    </StackPanel>
                </UniformGrid>

                <UniformGrid Columns="3" Margin="0,10,0,0">
                    <StackPanel Margin="10,5">
                        <TextBlock Text="جهة اتصال الطوارئ" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SelectedPatient.EmergencyContact}" Height="38" VerticalContentAlignment="Center" Padding="8" BorderBrush="#DCDFE6"/>
                    </StackPanel>

                    <StackPanel Margin="10,5">
                        <TextBlock Text="هاتف الطوارئ" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SelectedPatient.EmergencyPhone}" Height="38" VerticalContentAlignment="Center" Padding="8" BorderBrush="#DCDFE6"/>
                    </StackPanel>

                    <StackPanel Margin="10,5">
                        <TextBlock Text="العنوان" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SelectedPatient.PersonInfo.Address}" Height="38" VerticalContentAlignment="Center" Padding="8" BorderBrush="#DCDFE6"/>
                    </StackPanel>
                </UniformGrid>

                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="10,5">
                        <TextBlock Text="الحساسية (Allergies)" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SelectedPatient.Allergies}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="8" BorderBrush="#DCDFE6"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,5">
                        <TextBlock Text="التاريخ المرضي (Medical History)" Foreground="#5D7A95" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SelectedPatient.MedicalHistory}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="8" BorderBrush="#DCDFE6"/>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,20,0,0">
                    <Button Command="{Binding SavePatientCommand}" Content="حفظ البيانات" Width="150" Height="40" Background="#2ECC71" Foreground="White" FontWeight="Bold" Margin="0,0,10,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Command="{Binding ClearFieldsCommand}" Content="تفريغ الحقول" Width="100" Height="40" Background="#BDC3C7" Foreground="White" FontWeight="Bold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="15" Margin="20,10,20,20" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="سجل المرضى المسجلين" FontSize="18" FontWeight="SemiBold" Foreground="#34495E" Margin="10,5,10,15"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Patients}" 
                          SelectedItem="{Binding SelectedPatient}"
                          AutoGenerateColumns="False" CanUserAddRows="False"
                          Style="{StaticResource DGStyle}" 
                          ColumnHeaderStyle="{StaticResource DGHeader}" 
                          RowStyle="{StaticResource DGRow}" 
                          CellStyle="{StaticResource DGCell}">
                    <DataGrid.Columns>

                        <DataGridTextColumn Header="الاسم" Binding="{Binding FullName}" Width="300">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="10,2"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#2C3E50"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="فصيلة الدم" Binding="{Binding BloodType}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="جهة الطوارئ" Binding="{Binding EmergencyContact}" Width="170">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="5,2"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="هاتف الطوارئ" Binding="{Binding EmergencyPhone}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="5,2"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="تاريخ التسجيل" Binding="{Binding CreatedDate, StringFormat=\{0:yyyy/MM/dd\}}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="الحالة" Binding="{Binding IsActive}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="True">
                                            <Setter Property="Foreground" Value="#2ECC71"/>
                                            
                                        </Trigger>
                                        <Trigger Property="Text" Value="False">
                                            <Setter Property="Foreground" Value="#E74C3C"/>
                                            
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>